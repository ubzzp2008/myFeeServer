<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper  namespace="com.jnc.web.dao.userinfo.ISzkDao">

	<resultMap type="szkDemo" id="demoResult">
			<result	property="code"	column="code"	/>
			<result	property="name"	column="name"	/>
	</resultMap>

    <insert id="saveSzk" parameterType="SzkDemo">
        insert into bsap.zzp_temp15 (code,name)
        values(#{code},#{name})
    </insert>
    <!--<select id="queryAll" resultMap="demoResult"  parameterType="easyPoiDemo">
        select uidstr,
        mandt,
        boxflag,
        matnr,
        vtweg
        from
            bsap.zzp_wlm
        <where>
            <if test="flag!=null">
                and flag = #{flag}
            </if>
			&lt;!&ndash;<if test="kunnr!=null">
				and kunnr = #{kunnr}
			</if>&ndash;&gt;
            <if test="vtweg!=null">
                and vtweg = #{vtweg}
            </if>
			<if test="matnr!=null">
				and matnr = #{matnr}
			</if>
		</where>
    </select>-->
</mapper>